input {
    beats {
        port => 5044
        codec => json
    }
}

output {
  stdout {codec => rubydebug}
  if [source] =~ "getprop" {
    elasticsearch{
      hosts => ["127.0.0.1:9200"]
      index => "index-name-deviceinfo"
      document_type => "getprop"
    }
  }
}